experiment:
  name: criteo_fwfm_bspline
  variant: bspline_integer_basis

model:
  integer:
    bspline:
      normalize_fn: clamp
      normalization_scale: 1.0
      degree: 3
      knots_config: 16
      input_map: log1p_cap_to_unit
      out_min: -1.0
      out_max: 1.0
