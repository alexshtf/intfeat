experiment:
  name: criteo_fwfm_medium

data:
  split:
    mode: tail_holdout
    train_rows: 1000000
    val_rows: 200000
    test_rows: 200000

train:
  batch_size: 8192
  num_epochs: 10
  early_stopping:
    patience: 2
